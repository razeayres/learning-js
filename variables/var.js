/**
 * this shows how to store
 * a value in a variable
 * using the declaration var
 */
var myVar = 1

/**
 * with it, we can define and
 * declare multiple variables
 * at once
 */
var a = 1, b = "two", c = false

/**
 * it is possible to only
 * declare the variable without
 * defining it
 */
var empty

/**
 * using the statement "var" is
 * not strictly necessary. We
 * can directly do without it
 * and it is still a var. This
 * is also used this way for
 * reassignments
 */
myVar = 2